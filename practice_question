// Write a program to input an integer N and print the sum of all its even digits and sum of all its odd digits separately.Digits mean numbers, not the places! That is, if the given integer is "13245", even digits are 2 & 4 and odd digits are 1, 3 & 5.Input format :
// Integer N

// Output format :
// Sum_of_Even_Digits Sum_of_Odd_Digits
// (Print first even sum and then odd sum separated by space)

// Constraints
// 0 <= N <= 10^8

// Sample Input 1:
// 1234

// Sample Output 1:
// 6 4

// Sample Input 2:
// 552245

// Sample Output 2:
// 8 15
#include <stdio.h>
// int main(){
//     float m1,n1;
//     float m2,n2;
//     scanf("%f%f",m1,n1);
//     scanf("%f%f",m2,n2);
//     float totalm1,totalm2;
//     totalm1=m1+n1/2;
//     totalm2=m2+n2/2;
//     printf("Crop 1: %.2f",totalm1);
//     printf("Crop 2: %.2f",totalm2);
//     (totalm1>totalm2)?printf("Crop 1 had the highest average yield"):printf("Crop 2: had the highest average yield ");
//     return 0;
// }
int main(){
    int n;
    printf("enter number : ");
    scanf("%d",&n);
    for(int i=0; i<n;i++){
        for(int j = 0 ; j < n+1 ; j++){
            printf("*");
        }
        printf("/n");
    }
}